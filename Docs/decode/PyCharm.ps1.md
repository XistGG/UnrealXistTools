# Decode Analysis: PyCharm.ps1

## Definition
**Path**: `PyCharm.ps1`

### Parameters
- `[Parameter()]$Path`: Optional directory path to open in PyCharm. Defaults to current directory.

## Usages
No usages found in the workspace (other than documentation/README).

## Invocation Details
Standalone tool to open PyCharm with P4 environment configured.

## Observations
- Helper script to launch PyCharm with the correct environment.
- Uses `[P4Config.ps1](../../P4Config.ps1)` to source environment variables.
